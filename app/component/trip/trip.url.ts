import { TRIP } from '../../lib/url';
export const GET_TRIP = `${TRIP}`;

export const GET_TRIPS = `${TRIP}s`;
export const BOOK_TRIP = `${TRIP}/book`;
export const PICK_DRIVER = `${TRIP}/select-driver`;
export const DAILY_CHECK_IN = `${TRIP}/check-in/daily`;
export const DAILY_CHECK_OUT = `${TRIP}/check-out/daily`;
export const WEEKDLY_CHECK_OUT = `${TRIP}/check-out/weekly`;
export const WEEKLY_CHECK_IN = `${TRIP}/check-IN/weekly`;
export const FARE_ESTIMATE = `${TRIP}/estimate-fare`;
export const DRIVERS_AND_FARE = `${TRIP}/drivers-and-fare`;
export const GET_RECENT_PICKUPS_ADDRESSES = `${TRIP}/recent-pick-ups`;
export const CONFIRM_TRIP = `${TRIP}/confirm`;
export const CANCEL = `${TRIP}/cancel`;
export const UPCOMING_TRIPS = `${TRIP}/upcoming`;
