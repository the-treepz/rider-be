import { TRIP } from '../../lib/url';

export const GET_TRIP = `${TRIP}`;
export const GET_TRIPS = `${TRIP}s`;
export const GET_CHECK_IN = `${TRIP}/check-in`;
export const GET_CHECK_OUT = `${TRIP}/check-out`;
