import { AUTH_URL } from '../../lib/url';

export const LOGIN_USER_URL = `${AUTH_URL}sign-in`;
export const SIGN_UP = `${AUTH_URL}sign-up`;
export const FORGOT_PASSWORD = `${AUTH_URL}forgot-password`;
export const CHANGE_PASSWORD = `${AUTH_URL}change-password`;
export const RESET_PASSWORD = `${AUTH_URL}RESET-password`;
